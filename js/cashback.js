function calculateCashback(regular, high, extra) {
    // * 0.01
    // * 0.05
    // * 0.3
{

    let purchaseAmount = (regular * 0.01)  + (high * 0.03)  + (extra * 0.3);
    for (regula, hig, extr of regular, high, extra) {
        if (regula )
    }
}


    // всё сложить

    // if -> итоговый результат > 3000?
    if
    let result = 0; // TODO: сосчитать кэшбек
    return result;
}

console.log(calculateCashback(30000, 10000, 15000));